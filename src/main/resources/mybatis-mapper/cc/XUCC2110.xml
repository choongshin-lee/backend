<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mom.backend.XUCC2110">
    <select id="get_findBtn1" parameterType="java.util.HashMap" resultType="com.mom.backend.dto.LowerHashMap" fetchSize="1000">
        SELECT MIMS.COMPANY_CD 
		     , MIMS.DIVISION_CD
		     , MIMS.ITEM_ID
		     , MI.ITEM_NM
		     , MI.ITEM_SPEC
		     , MI.ITEM_TYPE
		     , (SELECT MOM_COMMON_PKG.FN_GET_CODE_NAME(MI.COMPANY_CD, MI.DIVISION_CD, 'MD0002', MI.ITEM_TYPE, #{langCd, jdbcType=VARCHAR}) FROM DUAL)  AS ITEM_TYPE_NM
		     , MIMS.UOM
		     , (SELECT MOM_COMMON_PKG.FN_GET_UNIT_NAME(MIMS.COMPANY_CD, MIMS.DIVISION_CD, MIMS.UOM) FROM DUAL) AS UOM_NM
		     , MIMS.CLOSE_YEAR_MONTH                
		     , TO_CHAR(MIMS.IO_DATE ,'YYYY-MM-DD')  AS CLOSED_DATE
		     , MIMS.WAREHOUSE_CD
		     , MWH.WAREHOUSE_NM  
		     , MWH.PLANT_CD
		     , (SELECT MOM_COMMON_PKG.fn_get_plant_name(MWH.COMPANY_CD, MWH.DIVISION_CD, MWH.PLANT_CD) FROM DUAL) AS  PLANT_NM
		     , MIMS.BEGIN_QTY
		     , MIMS.IN_QTY
		     , MIMS.OUT_QTY
		     , MIMS.END_QTY
		     , MIMS.DESCRIPTION
		     , ROWIDTOCHAR(MIMS.ROWID)  AS KEY_ID
		FROM   MOM_ITEM_INOUT_MONTHLY_SUMMARY MIMS
		     , MOM_ITEM MI 
		     , MOM_WAREHOUSE MWH
		WHERE MIMS.COMPANY_CD       = MI.COMPANY_CD 
		AND   MIMS.DIVISION_CD      = MI.DIVISION_CD 
		AND   MIMS.ITEM_ID          = MI.ITEM_ID 
		AND   MIMS.COMPANY_CD       = MWH.COMPANY_CD 
		AND   MIMS.DIVISION_CD      = MWH.DIVISION_CD 
		AND   MIMS.WAREHOUSE_CD     = MWH.WAREHOUSE_CD 
		AND   MIMS.COMPANY_CD       = #{companyCd, jdbcType=VARCHAR}
		AND   MIMS.DIVISION_CD      = #{divisionCd, jdbcType=VARCHAR}
		AND   MIMS.CLOSE_YEAR_MONTH = #{closeYearMonth, jdbcType=VARCHAR}
		<if test="itemType != null and itemType != ''">
		AND     MI.ITEM_TYPE    IN (${itemType}) 
		</if>
		<if test="itemIdNm != null and itemIdNm != ''">
		AND (UPPER(MI.ITEM_ID) LIKE '%' || UPPER(#{itemIdNm, jdbcType=VARCHAR}) || '%' OR UPPER(MI.ITEM_NM) LIKE '%' || UPPER(#{itemIdNm, jdbcType=VARCHAR}) || '%')	  
		</if>
    </select>
    <select id="proc_customBtn1-1" statementType="CALLABLE" parameterType="java.util.HashMap" resultType="com.mom.backend.dto.LowerHashMap">  
    { CALL MOM_CLOSED_PKG.SP_CREATE_ITEM_INOUT_M_SUMMARY( #{p_err_code,     jdbcType=VARCHAR, mode=OUT}
					                                          , #{p_err_msg,      jdbcType=VARCHAR, mode=OUT}
					                                          , #{companyCd,      jdbcType=VARCHAR, mode=IN}
					                                          , #{divisionCd,     jdbcType=VARCHAR, mode=IN}
					                                          , #{closeYearMonth, jdbcType=VARCHAR, mode=IN}													  
					                                          , #{userId,         jdbcType=VARCHAR, mode=IN} )}       	              
    </select>    
</mapper>